import {StyleSheet} from 'react-native';
import {sizes, colors} from '@constants';

export default StyleSheet.create({
  container: {},
  title: {
    fontWeight: '600',
    fontSize: sizes.pts_16,
    lineHeight: sizes.pts_24,
    color: colors.white_FFFFFF,
  },
});
