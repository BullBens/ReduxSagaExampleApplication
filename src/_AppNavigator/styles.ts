import {StyleSheet} from 'react-native';
import {colors} from '@constants';

export default StyleSheet.create({
  headerStyle: {
    backgroundColor: colors.black_087,
    borderBottomColor: colors.white_FFFFFF,
    borderBottomWidth: 1,
  },
  headerTitleStyle: {
    color: colors.red_E50003,
  },
  cardStyle: {

  },
});
